{
  "dependencies": {
    "flatpickr": "^4.6.13"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.0.6"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  }
}
